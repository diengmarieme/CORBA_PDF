services:
  - type: web
    name: pdf-corba-middleware
    runtime: docker
    repo: https://github.com/diengmarieme/CORBA_PDF # Ton dépôt
    plan: free
    branch: main
    rootDir: .
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 8080
